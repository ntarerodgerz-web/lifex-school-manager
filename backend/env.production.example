# ═══════════════════════════════════════════════════
# School Manager — Production Environment Variables
# Copy this to .env and fill in real values
# ═══════════════════════════════════════════════════

# Server
PORT=5000
NODE_ENV=production

# Database — Option 1: Single connection string (Neon, Railway, Supabase)
# Get this from your Neon dashboard → Connection Details → Connection string
DATABASE_URL=postgresql://neondb_owner:xxxx@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require

# Database — Option 2: Individual variables (local PostgreSQL / VPS)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=school_manager
# DB_USER=school_manager_user
# DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# JWT (generate strong random strings — e.g. openssl rand -hex 64)
JWT_SECRET=CHANGE_ME_RANDOM_STRING_64_CHARS
JWT_REFRESH_SECRET=CHANGE_ME_ANOTHER_RANDOM_STRING_64
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Cloudinary (optional — for cloud image storage)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# PesaPal (live credentials — NOT sandbox)
PESAPAL_CONSUMER_KEY=CHANGE_ME
PESAPAL_CONSUMER_SECRET=CHANGE_ME
PESAPAL_BASE_URL=https://pay.pesapal.com/v3
PESAPAL_IPN_CALLBACK_URL=https://yourdomain.com/api/v1/pesapal/ipn

# Email Notifications (Gmail SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ntarerodgerz@gmail.com
SMTP_PASS=your_app_password
SMTP_FROM=School Manager

# Frontend URL (same domain in production since backend serves frontend)
CLIENT_URL=https://yourdomain.com
