# Frontend production environment
# In production, the frontend is served by the backend, so API calls go to /api/v1
# No need to set VITE_API_URL — the default "/api/v1" works perfectly
# If you ever deploy frontend separately, uncomment and set:
# VITE_API_URL=https://yourdomain.com/api/v1

